let colors = ["red", "white", "blue", "green", "yellow"];

// // function showColors(color) {
// //     console.log(color);
// // }

// colors.forEach(showColors);

colors.forEach((color, order) => console.log(`Index: ${order} , ${color}`));