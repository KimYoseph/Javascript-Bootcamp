// ForEach --> It is a method/function which is a part of arrats and it takes a callback;

let daysOfWeek = ["Mon", "Tues", 'Wed', "Thurs", "Fri", "Sat", "Sun"];

console.log(daysOfWeek[5]);

 for(var i = 0; i < daysOfWeek.length; i++) {
    console.log(daysOfWeek[i]);
}


//daysOfWeek.forEach((day, index) => console.log("Index: " + index + ", " + day));
